import React from 'react';

export const LibSampleContext = React.createContext({});

// export const useLibSampleContext = () => React.useContext(LibSampleContext);

// export default LibSampleContext;
